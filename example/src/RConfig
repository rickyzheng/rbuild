
menu "Menu from src folder" do
    group :NAMED_GROUP => "combiniation group" do
        config :TEST_TEMP => "just a test"
    end
end

# include all sub-folders RConfig files
source "*/RConfig"

target :MODULE_A => "a.c"
target "b.c"
